/**
 * @module core/prompts/templates
 * 职责：全部 LLM 任务的内置提示词模板（静态定义，运行前即可展示）
 *
 * 设计要点：
 * - 模板使用 {{varName}} 占位符，由 registry.renderTemplate 在运行时替换为实际值
 * - 用户在 PromptLab 中直接编辑此模板（而非额外的"覆盖"层）
 * - 运行日志记录渲染后的提示词（占位符已替换为实际值），供用户查看
 * - 复杂格式化（数组→文本、JSON 序列化等）由调用方在 _vars 中预先完成
 */

export const TEMPLATES = {
  // =================================================================
  // 抽取类任务
  // =================================================================

  'doc-type-detect': {
    system: '',
    user: `你是一名文档分类专家。请根据以下文本片段判断文档类型和学科领域。

只返回合法 JSON，格式：
{"docType": "textbook|exam|paper|notes|slides|other", "domain": "学科领域", "specialElements": ["公式", "例题", "选择题", "证明"]}

textbook=教材/讲义，exam=考试卷/习题集，paper=学术论文，notes=笔记，slides=幻灯片，other=其他。

文本片段：
{{sample}}`
  },

  'cloud-term-extract': {
    system: '',
    user: `{{domainHint}}你是一名学术文本知识抽取专家。请从以下文本中提取关键知识点、核心概念和专业术语。

如果文本来自考试试卷，请提取题目涉及的核心考点/知识点。

要求：
- 术语/考点必须是文本中实际出现的完整词组
- 优先提取学科专属术语，避免过于宽泛的日常用语（如"方法""结果""问题"）
- 不要输出公式片段、数学符号或单独变量名
- 同一术语重复出现时只保留一次

输出严格的 JSON 数组：["术语1", "术语2", ...]
只返回 JSON 数组，不要任何解释。

文本内容：
{{batchText}}`
  },

  'term-refine': {
    system: '',
    user: `你是一名学术术语质量审核员。请审核以下从学术文档中抽取的术语。

文档开头内容（用于推断主题）：
{{topicHint}}

待审核术语：
{{termList}}

审核要求：
1. 判断每个术语是否是该文档领域的核心专业术语（是/否）
2. 如果术语不完整或表述不准，给出更准确的完整术语（如断词应补全为完整术语）
3. 如果术语过于宽泛（如"函数""方法""结果"等通用词，在任何领域都适用）、是数学变量/符号（如 EZ、EY、XY 等期望或变量写法）、或不是专业术语，标记为丢弃（keep=false）
4. 如果术语是完整的、准确的、领域相关的，保持原样（refined=原术语，keep=true）

特别提醒：
- 对于概率论与数理统计类文档/试题，"数学期望"、"方差"、"概率密度函数"、"分布函数"、"转移概率矩阵"、"状态空间"、"无偏估计"、"指数分布"、"检验统计量"、"严平稳过程"、"随机过程"、"分布律"等都是核心专业术语，应保留。
- 审核原则：保守优先。只要可能是该文档/学科的专业术语，就保留（keep=true）；只丢弃明显不是术语的片段或变量符号。
- 不要丢弃仅因"常见"就认为不重要的核心概念。

只返回合法 JSON 数组，每个元素对应一个术语（按上述序号）：
[{"index":1,"keep":true,"refined":"精炼后术语","reason":"简短理由"},...]

注意：index 从 1 开始；只返回 JSON，不要任何解释。`
  },

  'chunk-term-extract': {
    system: '',
    user: `你是一名学术文本知识抽取助手。请从下面这段文本中提取关键知识点、核心概念、专业术语或考试考点。

抽取要求：
1. 术语必须是文本中反复出现、被明确定义/解释，或在题目中考查的核心概念。
2. 每个术语必须是文本中实际出现的完整词组，不要生造、截断或拼接。
3. 优先抽取具有特定学科含义的专业术语，避免抽取泛化日常用语。
4. 如果文本是考试题/习题，请提取题目考查的"考点/知识点"，而不是题干本身。

允许抽取的类型（名词性术语）：概念、定义、定理、定律、性质、法则、方法、算法、模型、假设、检验、估计量、结构、系统、空间、函数、变量、参数、重要物理量、特征量、指标、考点。

不要输出：公式片段、数学符号、章节编号、普通动词、代词、泛化日常用语（如"方法""问题""结果"）、例题页码图表说明、单独字母或变量名。

正例（教材）：正态分布、假设检验、大数定律、马尔可夫链、协方差矩阵
正例（考试题）：正态分布、数学期望、标准差、概率密度函数、中心极限定理

输出要求：
- 同义表达只保留一个最具代表性的。
- {{countHint}}
- 只返回合法 JSON，格式如下，不要任何解释或额外文字：
{"domain": "学科领域", "terms": ["术语1", "术语2", ...]}

文本：
{{chunk}}`
  },

  'chunk-term-extract-strong': {
    system: `你是一名学术文本知识图谱构建专家，擅长从教材、论文、讲义中精准提取核心术语。

任务：阅读用户给出的文本，提取该学科领域的核心概念、定理、方法、模型等专业术语。

输出格式（严格 JSON，不要任何额外文字）：
{"domain": "学科领域", "terms": [{"term": "术语", "type": "概念|定理|方法|模型|其他", "importance": "高|中|低"}]}

规则：
1. 只提取文本中实际出现或被明确定义的术语，不生造
2. 同义表达只保留最具代表性的一个
3. 优先提取学科专属术语，避免泛化日常用语（如"方法""问题""结果"）
4. 术语长度 2-20 字符
5. 最多输出 {{maxTerms}} 个术语

示例 1（概率论）：
输入：正态分布是概率论中最重要的一类连续型分布，其概率密度函数为 f(x)=(1/σ√2π)e^{-(x-μ)²/2σ²}。中心极限定理指出，大量独立同分布的随机变量之和的分布近似正态分布。
输出：{"domain": "概率论与数理统计", "terms": [{"term": "正态分布", "type": "概念", "importance": "高"}, {"term": "中心极限定理", "type": "定理", "importance": "高"}, {"term": "概率密度函数", "type": "概念", "importance": "中"}, {"term": "独立同分布", "type": "概念", "importance": "中"}]}

示例 2（计算机科学）：
输入：哈希表通过哈希函数将键映射到数组位置，实现 O(1) 平均查找。冲突处理常用链地址法或开放寻址法。动态扩容在负载因子超过阈值时触发。
输出：{"domain": "计算机科学", "terms": [{"term": "哈希表", "type": "概念", "importance": "高"}, {"term": "哈希函数", "type": "方法", "importance": "高"}, {"term": "链地址法", "type": "方法", "importance": "中"}, {"term": "开放寻址法", "type": "方法", "importance": "中"}, {"term": "负载因子", "type": "概念", "importance": "中"}, {"term": "动态扩容", "type": "方法", "importance": "低"}]}

示例 3（考试题）：
输入：设随机变量 X ~ N(μ, σ²)，求 P(X > μ + 2σ)。
输出：{"domain": "概率论与数理统计", "terms": [{"term": "正态分布", "type": "概念", "importance": "高"}, {"term": "标准差", "type": "概念", "importance": "高"}, {"term": "概率计算", "type": "方法", "importance": "中"}]}`,
    user: `{{chunk}}`
  },

  'specificity-scoring': {
    system: `You output valid JSON only. No markdown, no reasoning text.`,
    user: `你是一位教材分析专家。下面列出了从文档中抽取的候选术语，以及每个术语所在的小标题。
请判断每个术语的"特异性"：
- 1-3 分：非常泛化，几乎整本书/文档都会提到的词
- 4-6 分：中等具体，是某章节主题但范围较广
- 7-10 分：非常具体，通常只在特定小节出现的概念、定理或方法

对每个术语输出 specificity（整数 1-10）和 isGeneric（1-3 分为 true，否则 false）。
请只输出 JSON 数组，不要输出任何解释。

候选术语：
{{batchText}}

输出格式：
[
  { "term": "示例术语", "specificity": 7, "isGeneric": false }
]`
  },

  'llm-chunking': {
    system: '',
    user: `你是一名文档结构分析助手。下面是一篇文档的段落摘要列表（按顺序）。
请决定应该在哪些段落之间切分，形成语义独立的块。通常每个主要章节、定理、方法对应一个块。

要求：
- 只输出要切分的段落索引列表（0-based）。
- 第一个索引必须是 0，表示从该段落开始一个新块。
- 不要解释，只输出 JSON 数组。

段落摘要：
{{summariesText}}

输出示例：[0, 5, 12]`
  },

  'term-normalize': {
    system: '',
    user: `你是一名学术术语规范化专家。请对下面这组术语进行规范化处理：

任务：
1. 同义合并：把相同概念的不同说法合并成一个最标准的术语。例如"正态分布"和"高斯分布"合并为"正态分布"。
2. 过滤明显不是术语的条目：仅删除纯泛化词（如"定义"、"方法"、"结果"、"例子"、"说明"、"结论"、"计算"）或单独出现的极通用词（如单独一个"函数"、"分布"、"变量"）。

重要规则：
- 只从输入列表中选择或合并，不要编造新术语。
- 保留复合术语中的核心词，例如"概率密度函数"、"分布函数"、"数学期望"、"转移概率矩阵"、"状态空间"、"无偏估计"、"指数分布"、"检验统计量"、"随机过程"、"严平稳过程"都是有效的专业术语，必须保留。
- 优先保留更通用的学术名称（如"正态分布"优于"高斯分布"）。
- 输出必须是合法 JSON 数组，不要任何解释。

输出示例：["标准化术语1", "标准化术语2", ...]

术语列表：
{{batchJson}}`
  },

  // =================================================================
  // 建图类任务
  // =================================================================

  'full-graph-extract': {
    system: `你是一个专业的文档结构分析助手。你需要阅读用户提供的完整文档文本，一次性提取以下内容并以 JSON 格式返回：

1. **多级标题树**（headings）：识别文档的章节结构（最多 {{maxHeadings}} 个），包括一级标题（如"第一章"、大节标题）和二级/三级标题。注意：
   - 只提取真正的章节标题，不要把正文中的编号列表（如"1. 细菌""2. 病毒"）误判为标题
   - 标题通常较短（< 50字）、独立成行、前后有空行或字号较大
   - 正文中的举例编号（如"例 1""3.2.1 某某"后面跟的是段落而非标题）不应被提取
   - 每个标题需要标注 level（1=顶层，2=二级，3=三级）和 start（在原文中的大致字符偏移）

2. **关键实体/术语**（entities）：提取文档中的核心概念、专业术语、定理、方法等（最多 {{maxTerms}} 个）。注意：
   - 每个实体需要标注 specificity（1-10 分，分数越高越具体/专业）
   - specificity ≤ {{threshold}} 的视为"泛化词"（如"概率""方法""细菌"这类在整本书中反复出现但无深度的词），设 isGeneric=true
   - specificity ≥ 7 的视为"具体概念/定理/方法"（如"贝叶斯公式""革兰氏染色""内毒素"），设 isGeneric=false
   - 每个实体需要标注 start（在原文中首次重要出现的字符偏移，用于跳转定位）
   - 每个实体需要标注 heading（它所属的最近上级标题）
   - 每个实体需要标注 type（concept/method/theorem/structure/disease/organism/term 等适当类型）

3. **实体间关系**（relationships）：提取实体之间的语义关联，不强制数量要求，能确定多少就返回多少。要求：
   - 所有关系统一使用 "related" 类型，表示内容上的语义关联
   - 每条关系需要标注 weight（0-1，关联强度，0.9=强关联，0.5=中等关联）
   - 只返回原文中确实存在、较明确的关联，不要强行编造

返回格式（严格 JSON，不要包含其他文本）：
\`\`\`json
{
  "headings": [
    {"title": "标题文本", "level": 1, "start": 0}
  ],
  "entities": [
    {"term": "实体名称", "specificity": 8, "isGeneric": false, "start": 100, "heading": "所属标题", "type": "concept"}
  ],
  "relationships": [
    {"from": "实体A", "to": "实体B", "type": "related", "weight": 0.8}
  ]
}
\`\`\`

注意事项：
- start 是字符偏移量，请尽量准确但允许有少量误差
- 标题的 start 是标题文本在原文中的起始位置
- 实体的 start 是该实体在正文中（非目录）首次重要出现的位置
- 如果文档没有明确的标题结构，headings 返回空数组
- 如果文档中确实不存在明显实体关联，relationships 可以为空数组
- 不要编造原文中不存在的内容`,
    user: `以下是完整文档文本（共 {{textLength}} 字符）。请分析并返回 JSON 结构化结果：

{{markedText}}`
  },

  'relation-infer-r2': {
    system: `你是一个知识图谱关系推断专家。我会给你一组实体和原文，请推断实体之间的语义关系。

要求：
- 所有关系统一使用 "related" 类型，表示内容上的语义关联
- 每个实体至少有 2 条关系
- 关系必须基于原文内容，不要编造
- weight: 0.9=强关联, 0.7=中等, 0.5=弱关联
- 返回严格 JSON，不要包含其他文本

返回格式：
\`\`\`json
{"relationships": [{"from": "实体A", "to": "实体B", "type": "related", "weight": 0.8}]}
\`\`\``,
    user: `实体列表（共 {{entityCount}} 个）：
{{entityList}}
{{existingRelStr}}

原文（节选前 8000 字符）：
{{textExcerpt}}

请推断实体间的关系，返回 JSON：`
  },

  'crosslink-llm': {
    system: '',
    user: `你是一名学术文献分析助手。请判断以下来自不同文档的术语/标题/实体对是否在语义上相关。

背景：这些文档对的总体相关性如下（越高越可能相关）：
{{docInfo}}

候选列表：
{{lines}}

判断标准（请从宽判断，宁可多标相关也不要漏掉）：
- 相关：讨论同一概念、同一方法、同一现象、同一学科领域，或存在上下位、因果、组成、类比、应用关系。
- 相关：共享核心关键词（如"细菌"与"革兰阳性菌"，"病毒"与"流感病毒"，"免疫"与"抗体"）。
- 相关：一个是另一个的应用场景/实例（如"PCR"与"病毒检测"）。
- 相关：标题结构不同但主题相同（如"细菌的形态与结构"与"细菌结构"）。
- 不相关：主题完全不同，且没有任何可识别的概念交集。

只返回合法 JSON 数组，每个元素对应一个候选对（按上述序号）：
[{"index": 0, "related": true, "score": 0.8, "reason": "简短理由"}, ...]

注意：
- index 从 0 开始
- score 为 0-1 的相关度
- 只要有一定关联就标 related=true，score 可给 0.5-0.7
- 只返回 JSON，不要任何解释`
  },

  // =================================================================
  // 考题类任务
  // =================================================================

  'exam-detect-llm': {
    system: `你是一名文档类型判断助手，只输出合法 JSON，不要任何解释。`,
    user: `请判断以下文本片段来自一份"考试试卷/习题集"，还是来自"教材/讲义/笔记"。

判断标准：
- 考试试卷：片段开头通常有"XX试卷"、"考试课程"、"考试时间"、"密封线"、"得分/评卷人"等抬头，且文档主体由大量独立考题组成。
- 教材/讲义：主体是知识点讲解、定义、定理，题目只是例题、课后练习或案例分析，数量较少。

请只输出合法 JSON，不要任何解释：
{
  "isExamPaper": true/false,
  "confidence": 0.0-1.0,
  "reason": "简要理由"
}

文本片段：
"""
{{sample}}
"""`
  },

  'exam-concept-extract': {
    system: '',
    user: `你是一名课程助教。请分析下面每一道试题，指出它真正考查的"知识点/考点概念"（上位概念）。

要求：
1. 每道题输出 1-3 个核心考点概念，优先使用教材中的标准术语。
2. 如果题目考查的是某个公式/性质/定理的应用，请输出该公式/性质/定理的名称，而不是泛泛的词。
3. 如果题目涉及多个概念，输出最重要的那个，可附带 1-2 个相关概念。
4. 不要输出题目中直接出现的具体数值、变量名或选项字母。
5. 只输出合法 JSON 数组，不要任何解释。

输出格式示例：
[
  {"questionNumber":"1","concept":"概率加法公式","related":["事件概率"],"importance":1.0,"reason":"由并集概率反求交集概率"},
  {"questionNumber":"2","concept":"数学期望","related":["二项分布"],"importance":1.0,"reason":"求停车次数的期望"}
]

试题如下：

{{questionText}}`
  },

  // =================================================================
  // 结构类任务
  // =================================================================

  'cloud-heading-extract': {
    system: '',
    user: `你是一名文档结构分析专家。我会给你一篇文档的第 {{startPage}} 到第 {{endPage}} 页文本，请识别其中所有的结构性标题。

输出严格的 JSON 数组，每个元素格式：
{"title": "标题原文", "level": 1-6, "page": 页码}

层级规则：
- level 1：最高级（如"第1章 xxx"、"Part I xxx"、"第一章"）
- level 2：中级（如"1.1 xxx"、"第二节"）
- level 3：小节（如"1.1.1 xxx"、"（一）xxx"、"A. xxx"）
- level 4-6：更细的子标题、例题、步骤（如"例题 1"、"Step 2"）

判断标准（请结合上下文判断，不要只看单行的编号前缀）：
- 标题通常是有编号前缀的短行，概括后续内容。
- 章节编号（如 1.2、第二章、(a)、A.）应完整保留在 title 中。
- 考试卷中的大题号（如"一、填空题"、"二、选择题"）是标题。
- 例题/步骤标记（如"例题 1"、"Step 2"）如果引领一段内容，可视为低层级标题。
- 正文句子、公式行、列表项、页码、目录项不是标题。
- 选择题的 A/B/C/D 选项（如"A. 选项一"、"B) xxxx"、"(C) xxxx"）不是标题；如果连续多行是 A/B/C/D 编号，它们都是选项。
- 如果同一标题在本批中多次出现，只保留第一次。

请只返回 JSON 数组，不要任何解释文字、不要返回 Markdown 代码块、不要返回 LaTeX。

示例：
[
  {"title": "一 选择题", "level": 2, "page": 2},
  {"title": "三 解答题", "level": 2, "page": 4},
  {"title": "(2) X + Y 的概率密度函数", "level": 4, "page": 4}
]
其中 "(2) X + Y 的概率密度函数" 如果是大题下的第(2)小问，则不应作为标题。

文本内容：
{{batchText}}`
  },

  'heading-refine': {
    system: '',
    user: `你是一名文档结构分析专家。下面是从文档中提取的候选标题，每个候选都附带原文中的前后几行上下文。
请根据上下文判断每个候选是否真的是章节标题，并修正层级（1-6，1为最高级）。

判断标准：
- 标题通常是简短的名词性短语，概括后续段落的主题。
- 章节编号（如"1 引言"、"2.3 方法"、"第三章"、"（一）背景"、"A. 方法"）是标题。
- 考试卷中的大题号（如"一、填空题"、"二、选择题"、"三、解答题"）是标题。
- 例题/步骤标记（如"例题 1"、"Step 2"）如果引领一段内容，可视为低层级标题。
- 试卷中具体的小题号（如"1. 设 X..."、"(1) 求 X..."、"(2) X + Y 的概率密度函数"）不是标题，它们是题目内容。
- 选择题的 A/B/C/D 选项（如"A. 选项一"、"B) xxxx"、"(C) xxxx"）不是标题。
- 正文句子、公式、图表说明、目录项、页码、页眉页脚不是标题。
- 如果连续多行都是 A/B/C/D 编号，它们都是选项。

输出格式：
请只返回 JSON 数组，每个元素格式：{"index": 序号(从1开始), "isHeading": true/false, "level": 1-6}
不要返回任何解释文字、不要返回 Markdown 代码块、不要返回 LaTeX。

示例（假设候选列表依次是"三 解答题 (本题满分 12 分)"、"(2) X + Y 的概率密度函数"、"2) Z 的概率密度函数"、"A. 正态分布期望为 0"）：
[
  {"index": 1, "isHeading": true, "level": 2},
  {"index": 2, "isHeading": false, "level": 1},
  {"index": 3, "isHeading": false, "level": 1},
  {"index": 4, "isHeading": false, "level": 1}
]
解释：大题号"三 解答题"是标题；"(2)"、"2)"、"A."这些具体小题/选项不是标题。

候选列表：
{{blocks}}`
  },

  'heading-discover': {
    system: '',
    user: `你是一名文档结构分析专家。以下是从文档中提取的"可疑行"，每个都附带前后几行原文上下文。
请结合上下文判断哪些确实是标题/章节名（而非正文句子、公式、列表项、页码、选择题选项等），并给出层级（1-6，1为最高级）。

判断标准：
- 标题通常是简短的名词性短语，概括后续段落的主题。
- 章节编号（如"1 引言"、"2.3 方法"、"第三章 概率论"、"（一）背景"、"A. 方法"）是标题。
- 考试卷中的大题号（如"一、填空题"、"二、选择题"）是标题。
- 考试题型标记（如"例题 1"、"Question 2"、"Step 1"）如果引领一段内容，也可视为低层级标题。
- 试卷中具体的小题号（如"1. 设 X..."、"(1) 求 X..."）不是标题。
- 正文句子、公式、例题内容、图表说明、目录项、页码不是标题。
- 选择题的 A/B/C/D 选项（如"A. 选项一"、"B) xxxx"、"(C) xxxx"）不是标题；如果连续多行是 A/B/C/D 编号，它们都是选项。

输出格式：
请只返回 JSON 数组，每个元素格式：{"index": 序号(从1开始), "isHeading": true/false, "level": 1-6}
不要返回任何解释文字、不要返回 Markdown 代码块、不要返回 LaTeX。

示例：
[
  {"index": 1, "isHeading": true, "level": 2},
  {"index": 2, "isHeading": false, "level": 1}
]

可疑行列表：
{{suspiciousBlocks}}`
  }
};

/**
 * 获取指定任务的内置模板
 * @param {string} taskId
 * @returns {{system:string, user:string}|null}
 */
export function getBuiltinTemplate(taskId) {
  const t = TEMPLATES[taskId];
  if (!t) return null;
  return { system: t.system || '', user: t.user || '' };
}
